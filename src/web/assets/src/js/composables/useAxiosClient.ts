import axios from 'axios'

const ENDPOINT = window.api.cpUrl ?? 'https://localhost:8003'

export const useDownloadTemplate = ($payRunId) => {

    // const formValues = {
    //     'payRunId': $payRunId,
    //     'CRAFT_CSRF_TOKEN': window.api.csrf,
    //     'action': 
    // }

    // console.log(formValues)

    // axios({
    //     method: 'post',
    //     url: `${ENDPOINT}/actions/staff-management/download-template`,
    //     data: formValues
    // })

}