{%- set options = {
    title: options.title ?? '',
    action: options.action ?? '',
} -%}

<button type="submit" data-action="{{- options.action -}}" class="cursor-pointer rounded-md border border-transparent bg-indigo-600 disabled:bg-indigo-400 disabled:cursor-not-allowed px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 sm:w-auto">{{- options.title -}}</button>